name: CI

on:
  push:
    branches: [ validation ]
  pull_request:
    branches: [ validation ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: |
        echo "hello, world!"
